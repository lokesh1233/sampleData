from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='kmeans_cluster_income and spend',
    author='kmeans_cluster_income and spend',
    license='',
)
